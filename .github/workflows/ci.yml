name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

env:
  FORCE_COLOR: 2

jobs:
  ci:
    runs-on: windows-latest

    steps:
      - name: Clone repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Build
        run: CALL "build.bat"
        shell: cmd

      - name: Prepare files
        run: |
          mkdir dist
          cp -r doc/ build/optipng.exe LICENSE.txt README.md dist/
        shell: bash

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: "optipng"
          path: dist/
          if-no-files-found: error
